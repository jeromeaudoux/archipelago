{
  "name": "am-islands-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone gene → AlphaMissense saturation heatmap + AM islands + ClinVar + protein domains viewer",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "precompute": "python3 scripts/run_all.py"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
